# Weather API Configuration (Optional)
# The app uses Open-Meteo API by default which is free and doesn't require an API key.
# These variables are for future API integrations if needed.

VITE_WEATHER_API_KEY=
VITE_AQI_API_KEY=
